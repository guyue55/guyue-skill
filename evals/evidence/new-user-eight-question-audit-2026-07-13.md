# New-User Eight-Question Audit - 2026-07-13

## Scope

- Reviewer: independent Codex CLI read-only session
- Runtime: Codex CLI 0.144.1, model `gpt-5.6-sol`
- Mode: repository read-only, no network, no file changes
- Questions: who uses Guyue, why install it, how to trigger it, what it delivers, where it is stronger, how it is proved, why a route wins, and where evidence comes from

## Initial Verdict

`FAIL`

| Question | Result | Evidence-backed conclusion |
|---|---|---|
| Who uses it | PASS | README defines complex-engineering, multi-Skill/runtime, and evidence-sensitive users; this is positioning, not adoption evidence. |
| Why install it | FAIL | Versioned routing, recovery, evidence, and gates are a coherent value proposition, but no real-user or controlled baseline proves superior outcomes. |
| How to trigger it | PASS | Explicit natural language and implicit narrowest-route selection are documented; 26 Codex canaries are runtime records, not universal runtime proof. |
| What it delivers | FAIL | Deliverable contracts are visible, but output replay covers only four synthetic profile representatives, not all 26 domains. |
| Where it is stronger | FAIL | The Prompt/Skill/Guyue comparison is a product claim without a controlled Prompt, single-Skill, or ordinary-Agent baseline. |
| How it is proved | FAIL | Deterministic tests are reproducible; the original 26 Codex event streams were not retained, and the first receipt exposed only hashes for profile output and review. |
| Why a route wins | PASS | A live read-only route explanation selected `reality-auditor` from `只读审查`; project routes were rejected without context. |
| Evidence source/version/boundary | FAIL | Receipts identify runtime, model, time, dirty baseline, hashes, and limits, but the candidate has no release commit/tag and no current public-install proof. |

## Findings And Disposition

- `P1` The 26 raw activation streams are not in the repository. Their receipts remain useful execution records but are not independently replayable artifacts. This boundary is retained; no retroactive PASS is claimed.
- `P1` The final E1-E4 output and independent review still existed locally with matching recorded hashes. They are now retained under `evals/evidence/artifacts/`, and `check_capability_chain.py` must recompute both hashes.
- `P1` All-Skill output quality, controlled comparative advantage, real-user value, cross-runtime behavior, and current public release remain unverified reality boundaries.
- `P2` README said 17 behavior contracts while the machine file and Showcase said 19. README is corrected to 19.
- `P2` The current candidate is intentionally unreleased. A dirty baseline receipt cannot be promoted into commit, tag, or public-install proof.

## Post-Fix Status

The two locally repairable audit defects are closed: profile artifacts are independently hash-checkable, and the public behavior-contract count is consistent. The overall audit remains `FAIL` because real-user value, comparative baselines, all-domain output quality, raw activation event retention, cross-runtime behavior, and public release cannot be manufactured by repository edits.
