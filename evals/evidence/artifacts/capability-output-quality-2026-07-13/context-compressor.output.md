[Trace: Guyue/ContextCompressor] 触发上下文预算管家：先定位浪费源，再选择最小上下文方案。

当前基线均来自用户描述，尚未经过 tokenizer 实测：

| 浪费源 | 当前基线 | 压缩方案 |
|---|---:|---|
| 日志 | 2,000 行/轮 | 本地先聚合与关键词定位，只注入错误类型、计数及命中行前后窗口；保留文件名、行号和检索命令 |
| Skill | 26 个全文/轮 | 常驻只保留名称与描述；按任务触发后才加载对应 `SKILL.md` 全文及必要引用 |
| MCP schema | 80 个/轮 | 只启用当前任务需要的工具族；优先改为“搜索工具 + 执行工具”两段式 |
| 历史会话 | 持续累积 | 每轮收敛为“当前事实、已排除、下一步、风险边界” |
| 模型输出 | 未提供 | 默认只输出结论、行动、阻断和证据索引 |

每轮实际输入包建议固定为：

1. 当前目标与权限边界。
2. 日志摘要、命中窗口及原始行号。
3. 本轮触发的 Skill 全文。
4. 本轮允许使用的 MCP schema。
5. 上轮检查点：事实、已排除项、未决风险、下一步。

日志采用渐进读取：先统计错误签名和频次，再读取命中窗口；证据不足时扩大窗口或定点读取，不直接退回全部 2,000 行。Skill 和 MCP 同样按需扩展，任务变化时重新路由。

度量方法：

```text
T0 = 原始完整请求的输入 Token
T1 = 压缩后请求的输入 Token

节省 Token = T0 - T1
节省率 = (T0 - T1) / T0 × 100%
```

应保存模型实际收到的序列化请求，并使用同一模型对应的 tokenizer 分别测量 `T0`、`T1`。同时按来源拆分：

```text
Δ日志   = T日志全文 - T日志摘要与窗口
ΔSkill  = T26个全文 - T本轮触发Skill
ΔMCP    = T80个schema - T本轮工具schema
Δ历史   = T完整历史 - T检查点
```

验证采用最多两轮“测量 → 调整 → 复测”：

- 比较相同任务、相同模型下的输入 Token。
- 检查关键错误、权限边界、风险和用户约束是否遗漏。
- 随机回查压缩摘要对应的原始日志行。
- 记录工具选择是否正确、任务是否一次完成。
- 若连续两轮边际节省低于预设阈值，停止继续压缩。

当前不能给出真实节省比例：缺少三类原始内容、实际序列化请求及对应 tokenizer。最小下一步是采集一个真实回合的压缩前请求，执行上述路由后再采集压缩后请求，用同一 tokenizer 计数；在此之前，所有比例只能标注为“预估”。
