[Trace: Guyue/SecurityGate] 对明确目标执行本地启发式预检；不把未命中等同完整安全

## 安全预检结论：Red（阻断）

`curl -s https://example.invalid/install.sh | bash` 会将未经审阅、未固定版本的远程内容直接交给 Shell 执行。脚本内容未知，无法确认其文件、网络、权限、持久化、凭证访问或数据外发行为。当前不得执行或放行。

### 威胁面板

- **高危执行模式**：远程响应直接进入 `bash`。
- **来源不可验证**：未提供可信发布来源或身份信息。
- **内容不可审计**：脚本正文未知。
- **版本不可复现**：未固定 commit、版本或内容摘要。
- **错误不可见**：`curl -s` 隐藏诊断信息，且未使用 `--fail`。
- **响应替换风险**：同一 URL 后续可返回不同内容。

### 最小 E4 收据

- 目标来源：`https://example.invalid/install.sh`
- 目标形式：远程安装脚本，经管道直接传给 `bash`
- commit/version/SHA-256：未提供，无法取得
- 扫描规则：仓库 `security-gate` 规则；静态审查给定命令片段
- 实际扫描命令：未运行（用户禁止联网与执行）
- 退出码：不适用
- 命中项：未知远程内容、未固定指纹、直接 Shell 执行
- 人工判断：高危执行模式，阻断
- 结论完整度：`partial`
- 未覆盖边界：脚本正文、重定向链、TLS/发布者身份、下载产物、运行时权限及副作用

[Trace: Guyue/SecurityGate] 发现高危行为并阻断；等待移除、隔离或明确风险处置。

最小下一步：提供离线保存的 `install.sh`、其可信来源说明以及发布方公布的 SHA-256。随后仅做静态审查和摘要核验；审查通过前不要执行。
